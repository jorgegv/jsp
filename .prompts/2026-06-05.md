# Tasks

## Task 1: cleaner build (moved from 2026-06-04 Task 5)

- Make all builds generate the binary artifacts, tap, dsk, etc. in a build/ directory instead of the repo root directory
- Fix the gitignore to ignore the build/ directory and its contents
- Adapt the "clean" target to clean that directory and remove the growing list of binary file names at the root (unneeded when using build/)
- No need to make the z88dk intermediate files be output to build/ (map, lis, .o, etc.). These can be kept with the source files as they currently are.

### Completion notes

- build/ dir for all artifacts; simplified .gitignore + clean. Commit 30e1097.

## Task 2: reorganize Makefile

- There are too many targets in the Makefile and they are confusing.
- Suggest me another organization for the explosion of the runr-* cpc-* explosion of targets.
- Consider that this is a top level Makefile.
- There may be other Makefiles in subdirs (e.g. in tests, or in cpc-cell-models) that may do some of the work independently of the main one (may be called separately) and called from the mail one if needed
- Propose a reorganization before starting work

### Completion notes

- Explored 3 candidate styles (Makefile.{param,macro,hybrid} + Makefile.common, kept as reference); not adopted (too complicated). Superseded by Task 3.

## Task 3: reorganize Makefile (2)

- COnsider the work done in the previous task, then:
- Keep the Makefile with just the following targets:
```
help
default
build -> alias to build-zx
build-zx
clean
run -> rename to zx-run
run-jnext -> rename to zx-run-jnext
profile -> rename: zx-profile
tests -> rename: zx-tests - run all zx tests
run-test -> rename: zx-run-test - parameter: test to run
cpc-tests -> run all cpc tests
cpc-run-test - parameter: test to run
bench -> rename: zx-bench. the next bench-*: same
bench-mask2
bench-sp1
bench-sp1-mask2
```

### Completion notes

- Curated top-level target set (zx-* prefixes + cpc-tests/cpc-run-test). Commits 5a94725, ab79be9.

## Task 4: automate zx regression tests

- There is a cpc-tests Makefile target that compiles and runs all tests for CPC
- The equivalent zx-tests target compiles but does not run the ZX tests with JNext in headless mode
- Make the zx target also run the regression tests, to have a complete regression test suite
- Review with an independent agent

### Completion notes

- zx-tests now builds + runs each test headless in JNEXT, diffs the frame-300 screenshot vs tests/refs/zx/ (magick AE; 0 = pass, any diff fails). 10 tests pass AE=0.
- Also made cpc-artifact-check a hard gate (fails on non-zero AE), symmetric with zx-tests (commit d06a198).
- Independent agent review APPROVE. Commit afb7d3d.

## Task 5: ensure documentation is completely in sync with code

- Analyze the source code and documentation with ultrathink and ensure the documentation reflects the current state of the software
- Do not touch any source code, only adjust documentation if needed
- Do not add more details to the documentation, unless you find something that is blatantly absent and should be documented
- Review with an independent agent

### Completion notes

- Fixed living docs only — ENGINE.md (stale CPC make targets → cpc-run-test/cpc-tests; pixel-cell default + memory-map framing), CPC-ASSETS-FORMAT.md (4 stale run-cpc-sprite-mode* → cpc-run-test), CPC-USAGE.md (cpc-sprite* ref).
- Historical records (cpc-target-tasklist.md, CPC-TARGET-PLAN.md, legacy/, .prompts/) left frozen by design; the two CPC design docs were later moved into doc/legacy/ (d06a198).
- Independent review APPROVE. Commit 2907110.

## Task 6: fix DEFINE_SPRITE macro

- Macro DEFINE_SPRITE should definitely accept sizes in pixels, not in rows/cols. 
- Since the compile mode is known at compile time, the macro can be redefined to accept pixel sizes and calculate the initialization values for rows and cols based on that. 
- Sizes should be multiples of 8 pixels. Can we ensure that at compile time?
- Review with independent agent
- Launch full regression tests for both platforms

### Completion notes

- DEFINE_SPRITE now takes pixel sizes (width_px, height_px — W×H; flipped from the initial height-first ordering in d06a198). rows/cols derived per-mode at compile time via JSP_SPRITE_ROWS/COLS + JSP_SPRITE_PPB (MONO=8). Multiple-of-8 enforced via a negative-sized typedef assert.
- All call sites migrated. ZX + CPC regressions pass AE=0; shift unit tests pass. Independent review APPROVE. Commits ba53182, d06a198.

## Task 7: add support in cpcgfx.pl for multicolor sprites/tiles

- Currently cpcgfx.pl only generates sprites/tiles with 2 colors
- Add support for exporting assets from a PNG file with 4/16 colors for CPC modes 1/0
- You'll need to add a RGB definition of the CPC color palette, and map the PNG colors to the closest colors in CPC palette
- Ensure that no more than 4 colors (mode 1) or 16 colors (mode 0) are used on a single asset
- Add an option to output the palette definition source code as well when exporting the asset to source code
- Create test PNG files similar to the ball.png one, but add some colors to the interior of the ball. Different PNGs for mode 0 and mode 1 (you'll add more colors to mode 0)
- Add Makefile targets for creating the ASM versions of these new assets with more colors (do not replace the old monochromatic ones)
- Modify the Mode0 and Mode1 tests to include some of these new assets so that we see them and they can be tested
- Update documentation with these new features

### Completion notes

- --multicolor maps each PNG pixel to the nearest of the 27 CPC hardware inks (RGB table added), assigns pens (pen0=bg, -m mask transparent, others first-appearance; errors >4 M1 / >16 M0); --palette-symbol emits the GA palette padded to the mode's pen count.
- Both 2-colour and multicolor share one pen-grid emit path; 2-colour output byte-identical (artifact AE=0). New assets/ball_m{0,1}.png + Makefile rules; mono assets not replaced.
- Mode0/1 tests show a multicolour ball (now moving), palette programmed from the emitted symbol. cap32-verified; CPCSPR0/1 refs regenerated (pixel+byte). cpc-tests green.
- Verified --multicolor also works for background tiles (-g tile), pixel-cell Mode 1. Added a mask-collision warning. Docs (CPC-USAGE, CPC-ASSETS-FORMAT) updated.
- Commits 90197ce, f5ae2e2, 8c39341. Confirmed complete by user.

## Task 8: make asset generation tools output 8 lines between sprite columns

- Make the gfx tools (both for CPC and ZX) generate sprites with 8 empty scanlines at start, end and between columns, instead of the current 7. 7 is more efficient memorywise, but our main user, RAGE1 assumes there are 8 blank lines.
- Perhaps some modifications are needed in the way some code skips to the next column's top scanline on the composition path (or may be not?)
- Do it for all platforms and screen modes

### Completion notes

- tools/gfxgen.pl (ZX) + tools/cpcgfx.pl (CPC): pre-rows and per-column trailing pad now 8 blank lines (was a 7-line overlapping pad). Engine column stride: rowstride = (rows+1)*cs (dropped the -(cs>>3) correction) in lib/{zx,cpc}/jsp_frame.asm; base/cs/kernels unchanged.
- Rendering is unchanged: the extra blank line is never composited (sub-cell-Y read reaches at most 7 lines past a column). Verified — exhaustive kernel read-analysis; a NOP experiment proving the regression refs are fragile to ANY code-size shift; CPC gated artifact regression AE=0; static-sprite ZX ref (test_sprite_draw) UNCHANGED + still AE=0 (would be impossible if compositing changed); visual clean circles.
- Regenerated the 8 padded sprite assets. Rebaselined deterministic-frame refs that shifted purely from binary-layout/frame-timing: 4 ZX + 6 CPC (other CPC sprite refs byte-identical). Docs (CPC-ASSETS-FORMAT.md) updated. cpc-tests green; zx-tests all 10 AE=0.
- Independent review APPROVE (very high confidence, no findings). Commit 1adef8f. Awaiting user confirmation.

## Task completion status

- [x] Task 1: cleaner build (moved from 2026-06-04 Task 5)
- [x] Task 2: reorganize Makefile
- [x] Task 3: reorganize Makefile (2)
- [x] Task 4: automate zx regression tests
- [x] Task 5: ensure documentation is completely in sync with code
- [x] Task 6: fix DEFINE_SPRITE macro
- [x] Task 7: add support in cpcgfx.pl for multicolor sprites/tiles
- [x] Task 8: make asset generation tools output 8 lines between sprite columns

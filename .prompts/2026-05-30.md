# Tasks

## Task 1: optimization with profiler

- Run `make profile` and let's analyze the first routine by time spent.
- We'll repeat several times with thehottest functions
- Use ultrathink and an expert profile for Z80 assembly or C if needed
- Let's iterate together, don't do it yourself. I want to understand the optimizations.

## Task 2: finish optimizations and summarize enhancements

1. Extend the cc_scratch optimization to LOAD kernels as well, and commit

2. Measure the total performance gains from this optimization round:
   - Checkout commit bed85eb76bde765aea9bf132ef0a2edb77310652 (just before starting)
     - Extract profile data from regular profile target (mixed LOAD and MASK sptires)
     - Modify the main program to use only LOAD sprites and extract profile data
     - Modify the main program to use only MASK sprites and extract profile data
   - Checkout this branch as it is now
     - Extract profile data from regular profile target (mixed LOAD and MASK sptires)
     - Modify the main program to use only LOAD sprites and extract profile data
     - Modify the main program to use only MASK sprites and extract profile data
   - Compare both datasets and give performance gains for:
     - LOAD sprites test
     - MASK sprites test
     - Mixed types test

## Task completion status

- [x] Task 1: jsp_redraw + jsp_covered + mask2 kernels optimized. Full-screen
      redraw -17.0% (1644->1364 bench frames), sprite-move -6.4% (3335->3122).
      Commits 8cb0479, 8a3367f, 0989a4a, af3f4f2 (loop-state-in-registers,
      char-row-base hoist, cell-index pass, mask2 dst absolute addressing).
      Reverted as net-neutral/negative: cols-cache, covered_cell row-sweep hoist.
- [x] Task 2:
   1. cc_scratch absolute-addressing extended to LOAD kernels (commit e043d12).
   2. Before/after comparison (baseline bed85eb vs current), fixed 300-frame
      test_sprite_move workload, profiler heatmap, per-frame redraw-pipeline
      T-states (one-time init + idle excluded; runs verified complete):
        workload   baseline      current     gain
        mixed      67,687,437    62,411,004   7.80%   (2 MASK2 + 3 LOAD1)
        mask       71,803,753    66,159,165   7.86%   (5 MASK2)
        load       64,612,370    59,670,173   7.65%   (5 LOAD1)
      Kernel-only slice (the cc_scratch target): mask 13.4%, mixed 15.2%,
      load 17.2%.  Per-function: _jsp_redraw 10.4%, covered_cell 6.0%,
      kernels 13-17%; blit/redraw_begin unchanged (not touched).
      Cross-checks with make bench (mixed sprite-move 3335->3088 = 7.4%).
